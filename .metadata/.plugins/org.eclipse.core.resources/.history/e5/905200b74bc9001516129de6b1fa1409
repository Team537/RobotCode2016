#ifndef SHOOTERMANAGER_HPP
#define SHOOTERMANAGER_HPP

#include <Schematic.hpp>
#include <Toolbox/MyButton.hpp>
#include <Vision/ShooterVision.hpp>

using namespace std;

class ShooterManager : public IComponent
{
private:
	ShooterVision *vision;
	MyButton *activateButton;
	bool activated;

public:
	ShooterManager(Joystick *joystick, ShooterVision *vision) : IComponent(joystick, new string("Shooter"))
	{
		this->vision = vision;
		activateButton = new MyButton(joystick, 10);
		activated = false;
	}

	void Update();
	void Dashboard();

	ShooterVision* GetVision() const
	{
		return vision;
	}

	bool IsActivated() const
	{
		return activated;
	}
};

#endif
